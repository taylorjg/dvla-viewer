name: CI/CD
on: push

jobs:
  run-unit-tests:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          node-version: "20"
      - uses: actions/setup-node@v3
        with:
          node-version: "20"
      - name: Clean install
        run: npm clean-install
      - name: Run lint
        run: npm run lint
      # - name: Run unit tests
      #   run: npm test
