import styled from "@emotion/styled";

export const StyledRegistrationPlate = styled.span`
  text-align: center;
  font: Verdana, sans-serif;
  font-size: 16pt;
  font-weight: 600;
  color: black;
  background-color: #ffd307;
  padding: 0.1rem 0.5rem;
  border-radius: 0.25em;
  margin: 0.25rem 0;
  display: block;
  width: fit-content;
`;
